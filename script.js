(()=>{const root=document.documentElement;const themeButton=document.getElementById('themeToggle');const topButton=document.getElementById('top');const dialog=document.getElementById('lightbox');const dialogImage=document.getElementById('lightImg');const dialogCaption=document.getElementById('caption');const closeButton=document.getElementById('close');const preferred=matchMedia('(prefers-color-scheme:dark)').matches?'dark':'light';const setTheme=theme=>{root.dataset.theme=theme;localStorage.setItem('theme',theme);document.querySelector('meta[name=theme-color]').content=theme==='dark'?'#1c1c1d':'#fff'};setTheme(localStorage.getItem('theme')||preferred);themeButton?.addEventListener('click',()=>setTheme(root.dataset.theme==='dark'?'light':'dark'));document.querySelectorAll('.project.compact').forEach(card=>card.style.gridTemplateColumns='1fr');const reveal=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.05});reveal.forEach(el=>observer.observe(el))}else{reveal.forEach(el=>el.classList.add('visible'))}addEventListener('scroll',()=>topButton?.classList.toggle('show',scrollY>400),{passive:true});topButton?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));document.querySelectorAll('.figure').forEach(button=>button.addEventListener('click',()=>{dialogImage.src=button.dataset.src;dialogImage.alt=button.dataset.caption;dialogCaption.textContent=button.dataset.caption;dialog.showModal()}));closeButton?.addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});})();